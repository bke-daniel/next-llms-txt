export const metadata = {
  title: 'Service Without Export',
  description: 'This service uses metadata fallback for llms.txt generation',
}

export default function NoExportPage() {
  return 'Service Without Export'
}
