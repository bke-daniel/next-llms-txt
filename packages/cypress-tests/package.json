{
  "name": "@next-llms-txt/cypress-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress E2E tests for next-llms-txt",
  "type": "module",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test:e2e": "concurrently -k -n server,cypress -c blue,green \"npm run server:dev\" \"npm run cypress:run\"",
    "test:dev": "concurrently -k -n server,cypress -c blue,green \"npm run server:dev\" \"npm run cypress:open\"",
    "server:dev": "npm run dev -w nextjs-test-server-app-router -- -p 3001"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cypress": "^15.6.0"
  }
}
