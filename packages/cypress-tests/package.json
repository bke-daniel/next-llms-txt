{
  "name": "@next-llms-txt/cypress-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress E2E tests for next-llms-txt",
  "type": "module",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test:e2e": "concurrently -k -s first -n server,cypress -c blue,green \"npm --prefix ../.. run server:test\" \"npm run cypress:run\"",
    "test:dev": "concurrently -k -n server,cypress -c blue,green \"npm --prefix ../.. run server:test\" \"npm run cypress:open\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cypress": "^15.6.0"
  }
}
