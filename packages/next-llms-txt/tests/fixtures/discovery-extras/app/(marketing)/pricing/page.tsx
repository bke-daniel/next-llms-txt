// Inside a route group: `(marketing)` is not part of the URL, so this page
// must be discovered at `/pricing`.
export const llmstxt = {
  title: 'Pricing',
  description: 'Page inside the (marketing) route group',
}

export default function PricingPage() {
  return null
}
