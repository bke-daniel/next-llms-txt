// Dynamic-segment route. The generated route should retain the literal
// `[id]` (Next.js routing convention) but route-derived TITLES should
// strip the brackets — P2 #36.
export const metadata = {
  description: 'Dynamic-route fixture',
}
export default function DynamicIdPage() {
  return null
}
