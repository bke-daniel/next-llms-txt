// Underscore-prefixed sibling file. Must NOT be picked up as a route.
export const llmstxt = {
  title: 'Meta',
  description: 'This should never appear in discovery output',
}
export default function Meta() {
  return null
}
